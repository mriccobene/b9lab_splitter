contract Splitter {
    uint constant N  = 2;
    address owner;
    address[N] partners;
    
    constructor(address[N] _partners) public {
        owner = msg.sender;
        for(uint i = 0; i < N; i++)
            partners[i] = _partners[i];
    }
    
    
}